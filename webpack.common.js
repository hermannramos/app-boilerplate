// Importar paquete de rutas absolutas (Ubicación partiendo desde la raiz del proyecto)
// Rutas relativas para una ubicación partiendo del archivo actual
const path = require('path');

// module.exports = {
//     entry:,
//     output: {
//         filename: 'bundle,js',
//         path: path.resolve(__dirname, './dist')
//     }
// }